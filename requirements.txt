# Laufzeit-Kern: bewusst stdlib-nah gehalten (Compliance-Fundament soll ohne
# schwere Abhaengigkeiten lauffaehig und pruefbar sein).
PyYAML>=6.0          # Konfiguration

# --- Optional / spaetere MVP-Stufen (auskommentiert, bis benoetigt) ---
# requests>=2.31.0          # API-Clients (Lexware, eBay, Bank-Aggregator)
# lxml>=5.0                 # robustes XRechnung/ZUGFeRD-XML-Parsing
# psycopg[binary]>=3.1      # PostgreSQL (Prod statt SQLite)
# python-dateutil>=2.9      # Datums-/Quartalslogik
